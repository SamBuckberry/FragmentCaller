install.packages("seqinr")
library(seqinr)

# Read in .fsa file
dat <- read.abif(filename="A01_913_HpaII_CA_AAG.fsa")
head(dat)
names(dat)


# Extract the data for the FAM lables

# Extract the data for the ladder

# Generate a plot of the data

